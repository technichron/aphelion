@segment text
    @global main

main: mov str, j
loop:
    mov $j, a
    cmp a, '\0'
    jif 1, return
    mov a, 0xffff
    add j, 1
    mov loop, p

return: hcf

@segment data
    str: string "hello world!\0"