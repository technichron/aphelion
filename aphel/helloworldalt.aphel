@segment text
    @global main

@define stdout, 0xffff

main: mov str, rJ # bruh
loop:
    mov $rJ, rA
    cmp rA, '\0'
    jif 1, return
    mov rA, stdout
    add rJ, 1
    mov loop, rP

return: jmp return

@org 0x0f00

@segment data
   str:   string "hello world!\0"